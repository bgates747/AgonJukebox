{
  "pass": true,
  "files_compared": 205,
  "method": "Rebuilt with frozen review/scripts snapshot into a separate temporary directory; all PNG/RGBA2222/AGNB/JSON artifacts matched byte for byte."
}
