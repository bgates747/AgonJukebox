# Shared skin schema evidence — 2026-09-13

1. regression.json: all 27 prior runtime files and all six app/test binaries
   byte-identical to 5df71d5; six reference renders have identical pixels.
2. repeat-build.json: all 44 generated files for the two migrated skins match
   an independent output-root build byte-for-byte.
3. third-proof.log: unchanged 26-scenario emulator harness passes, with 6,836
   widget and 6,922 font pixels, using generated data-only layout changes.
4. Unit validation: five test methods, including nineteen invalid-definition
   cases, duplicate keys, symlink escape and mismatched font backgrounds.

The proof is generated by scripts/skin_schema/prove_third.py. It changes rows,
field widths/placement, progress span and optional duration/count/arrow. It does
not edit player assembly. Source/outputs for the review are retained locally
under SKIN-020; generated proof artifacts are not a third shipped skin.

One repeat run under concurrent host validation failed the existing timed EOF
assertion. The original run and an isolated final run passed. This failure is
retained under the local task; no test deadline or assertion was weakened.
Fab also logs its existing libc++ mutex error when the host terminates the
headless process after PASS. Neither artifact establishes glitch-free audio.
No hardware access/deployment occurred. Review/promotion is pending.
