# Smooth-color-02 application artwork candidate

`artwork.png` and `artwork.svg` retain the second threshold-mask result: 564
simplified shapes colored from the earlier smooth concept, 512×384 with exact
Agon64 colors and no antialiasing/dithering. The SVG describes this source art.

`app-source.png` preserves the preceding central toolbar and replaces the
baked selection arrow with a continuous border. A separate 10×12 selection
sprite supplies the moving pointer in the application. `app-preview.png` shows
illustrative live fields and the Author's latest Concept 02 font. The
`*-without-pointer.png` images preserve the preliminary integration step.
The parent source files now use this accepted application composite; Git preserves the preceding artwork.

All 21 existing control-state images, including the volume bar, are retained
exactly. Graphics and multicolor font assets use AGNB; .font files supply metrics.
`provenance.json` identifies the artwork, preserved toolbar region, updated font,
sprite geometry and candidate binary. This is the accepted, incomplete progress checkpoint and supplies the default authoring inputs. The SVG does not include the raster-only
toolbar preservation/border repair in app-source.png.
